# Troubleshooting

- Check that the first line contains an HTTP method and a complete URL.
- Add an empty line between the headers and request body.
- Check your internet connection if a request cannot be sent.
- Verify authorization headers and SSL certificates when access fails.
- If the update check does not start, make sure `updater.exe` is installed next to `restclient.exe`.
