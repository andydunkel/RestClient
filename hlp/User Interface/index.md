# User Interface

The project tree on the left displays folders and `.rest` files. The upper editor contains the request, and the lower editor displays the response.

The menu and toolbar provide commands for opening projects, managing files, sending requests, and copying results.
