# Project Management

Open a folder to use it as a REST project. Only `.rest` files are displayed in the project tree.

Use the toolbar, menu, or context menu to create, rename, duplicate, move, and delete files or folders. Changes to the current request are saved automatically.

Recently opened project folders are available under **File → Recent Folders**.
