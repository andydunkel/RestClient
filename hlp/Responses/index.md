# Responses

The response editor displays the HTTP status, response headers, and response body.

Valid JSON responses are formatted automatically. Select **Request → Copy Result** to copy the complete response to the clipboard.
