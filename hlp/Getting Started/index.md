# Getting Started

1. Select **File → Open Folder** and choose a folder for your REST project.
2. Create or select a `.rest` file in the project tree.
3. Enter a request:

```http
GET https://postman-echo.com/get
Accept: application/json
```

4. Press `F5` or select **Request → Send**.

The response is displayed in the lower editor.
