# Examples

## GET request

```http
GET https://postman-echo.com/get
Accept: application/json
```

## POST request

```http
POST https://postman-echo.com/post
Content-Type: application/json

{
  "message": "Hello"
}
```

Other HTTP methods such as `PUT`, `PATCH`, and `DELETE` use the same format.
